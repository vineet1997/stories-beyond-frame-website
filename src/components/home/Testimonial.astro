---
// A single representative testimonial (Madalena "KIND WORDS" section).
// Placeholder couple name — swap with a real review.
---

<section class="kind section">
  <div class="container kind__inner reveal">
    <p class="eyebrow">Kind words</p>
    <blockquote class="kind__quote">
      “Where do we even begin? They saw moments we didn't even know were
      happening — the glance from my mother, my husband's quiet tears. Nothing
      felt staged, yet every single frame is one we'll keep forever. Honest,
      unhurried, and so beautifully done.”
    </blockquote>
    <p class="kind__by">— Aisha &amp; Rohan</p>
  </div>
</section>

<style>
  .kind__inner {
    max-width: 56rem;
    margin-inline: auto;
    text-align: center;
    display: grid;
    gap: 2rem;
    justify-items: center;
  }
  .kind__quote {
    font-size: var(--fs-h2);
    font-weight: 330;
    line-height: 1.32;
    text-wrap: balance;
  }
  .kind__by {
    font-family: var(--font-sans);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    font-size: 0.78rem;
    color: var(--stone);
  }
</style>
